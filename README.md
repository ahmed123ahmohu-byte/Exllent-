# Nexa

Futuristic AI web application with Arabic-first onboarding, premium chat UI, and smart code preview mode.

## Run locally

```bash
npm install
npm run dev
```

## Environment

Create `.env.local`:

```bash
OPENAI_API_KEY=your_key
```
